# Founder package — index & operating directive

Solo-founder, AI-first venture built on the event-time DeFi lending
allocator research. Generated by a multi-agent network (6 artifact agents
+ 1 YC-partner critic, web-researched), then reconciled against live data
by the orchestrator.

## Documents
- [`00_master_plan.md`](00_master_plan.md) — YC-canon execution arc (research → paper → product)
- [`01_market_competitors.md`](01_market_competitors.md) — TAM/SAM/SOM + 12-player competitor matrix
- [`02_icp_jtbd.md`](02_icp_jtbd.md) — ICP, jobs-to-be-done, 12-Q interview script, where to find users
- [`03_mvp_spec.md`](03_mvp_spec.md) — non-custodial advisory-first MVP, 3 tiers (dashboard → Safe one-click → guarded keeper)
- [`04_risk_compliance.md`](04_risk_compliance.md) — non-custodial regulatory wedge + risk register
- [`05_gtm_pitch.md`](05_gtm_pitch.md) — positioning, launch sequence, pricing, YC-app answers
- [`06_moat.md`](06_moat.md) — honest moat (event-time + zero-drift + reproducibility; NOT the ML)
- [`07_yc_partner_review.md`](07_yc_partner_review.md) — brutally honest critic; the operating directive

## Reconciled honest data state (orchestrator note, supersedes any stale doc)

The critic (code-only access) read a **stale** `PROJECT_OVERVIEW.md §10`
claiming "3-protocol, Spark/Compound/Fluid/DSR returned 0 events." That is
**outdated**. Verified against the live artefacts on disk (2026-06):

- The per-block panel is genuinely **6-way with real data**: Aave V3,
  Morpho Blue, Euler V2 (true per-block event streams) + Compound V3
  (`compound_v3_usdc_eth_*.parquet`, 12,904 hourly RPC rows), Spark
  (`spark_hourly.parquet`, 6,189 hourly), Fluid (`fluid_daily.parquet`,
  728 daily), DSR (`events_dsr.parquet`, 546). All forward-filled onto the
  per-block grid; ≥98.5% coverage; 0 NaN-accrual blocks.
- The honest caveat is **cadence heterogeneity** (3 per-block, Compound/
  Spark hourly, Fluid daily) — already disclosed in the paper and the
  research audit — **not** "3-of-6." The binding +1.5–2.8 pp result is a
  genuine 6-way-active walk-forward.
- The stale `PROJECT_OVERVIEW.md §10` is queued for the same correction
  already applied to the paper prose.

So the honest external claim is: **"6 protocols, real data, heterogeneous
refresh cadence; +1.5–2.8 pp leakage-free vs passive holds; the ML tier
adds nothing OOS — the simple gas-aware rule is the product."**

## Operating directive (from the critic — the riskiest assumption)

> Will a **named, reachable treasury** with a large idle USDC balance
> **pay** for better-than-passive yield delivered as a non-custodial
> signal, given the real (net-of-gas/slippage) edge is small below
> institutional size?

Everything is downstream of this. The two-week plan: **stop polishing,
ship the read-only Yield-Drag Report for 10 named treasuries on the
3 protocols we have the cleanest data for, with a real-gas counterfactual,
and put it in front of real humans.** Deliverable = interview notes, not
code. (See `07` §3.)

## What the orchestrator (AI) is doing autonomously
- Research integrity to completion (leakage + feature-wiring fixed; honest
  OOS verdict + gas-sensitivity sweep running) → see `docs/research/`.
- Paper honest sync (Phase B).
- Yield-Drag Report generator (Tier A MVP) — buildable from the existing
  replay engine + readers; the one thing worth building in the 2-week window.

## What only the human (founder) can do
Consolidated live and surfaced in chat at each cycle end. Headline set:
retain a crypto-securities lawyer; provide paid RPC + Etherscan/The Graph
keys; talk to ≥5 treasurers; pick the company name/entity/jurisdiction;
fund the eventual audit; decide the solo-vs-institutional-trust posture.
(Full list in `07` §4.)
