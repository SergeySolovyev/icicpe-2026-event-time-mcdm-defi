# Core framework (pinned to git tag — see README "Version pin" section for rationale)
# v1.3.2 is the latest GitHub release (2026-05-06) but has not been pushed to PyPI
# as of 2026-05-14, so we install directly from the git tag.
fractal-defi @ git+https://github.com/Logarithm-Labs/fractal-defi.git@v1.3.2

# Numerics & data
numpy>=1.26,<3
scipy>=1.13,<2
pandas>=2.1,<3
pyarrow>=15,<19
polars>=0.20,<1

# ML
torch>=2.1,<3
onnx>=1.15,<2
onnxruntime>=1.17,<2
catboost>=1.2,<2
scikit-learn>=1.4,<2

# Time series / classical models
statsmodels>=0.14,<1

# Experiment tracking (fractal-defi v1.3.2 requires mlflow>=3.11)
mlflow>=3.11,<4

# Data sources
requests>=2.31
gql[requests]>=3.5   # TheGraph GraphQL queries
dune-client>=1.7

# Utilities
tqdm>=4.66
loguru>=0.7
python-dotenv>=1.0
pyyaml>=6.0
einops>=0.7

# Plotting (for whitepaper figures)
matplotlib>=3.8
seaborn>=0.13

# Notebooks
jupyter>=1.0
nbconvert>=7.10

# Testing
pytest>=8.0
pytest-cov>=4.1
lifelines>=0.30
