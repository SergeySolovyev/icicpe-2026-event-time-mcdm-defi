# Walk-Forward Robustness

{% if not has_data -%}
**Note**: Walk-forward replay output is pending. This chapter will
populate once `results/institutional/tables/walk_forward.csv` is
generated by `scripts.dossier.walk_forward_validation` (24-cell replay
takes 30-60 min wall-clock).

The 6 non-overlapping 3-month windows are:

| Window | Start | End |
|---|---|---|
| W1 | 2024-11-01 | 2025-02-01 |
| W2 | 2025-02-01 | 2025-05-01 |
| W3 | 2025-05-01 | 2025-08-01 |
| W4 | 2025-08-01 | 2025-11-01 |
| W5 | 2025-11-01 | 2026-02-01 |
| W6 | 2026-02-01 | 2026-05-01 |

Inference design: paired bootstrap on N=6 per-window ΔSharpe deltas,
directional consistency count, regime-conditional split.
{%- else -%}
Six non-overlapping 3-month windows over the full panel (Nov 2024 –
Apr 2026). Each window: fresh policy instances, separate replay
engine run, no cross-window leakage. Aggregate inference: paired
bootstrap on the {{ n_windows }} per-window ΔSharpe deltas.

## Per-window Sharpe matrix

| Policy | {% for w in window_ids %}{{ w }} | {% endfor %}
|---|{% for w in window_ids %}---:|{% endfor %}
{% for policy, by_window in sharpe_by_policy.items() -%}
| {{ policy }} | {% for w in window_ids %}{% if by_window.get(w) is not none %}{{ "%.2f"|format(by_window[w]) }}{% else %}—{% endif %} | {% endfor %}
{% endfor %}

## Paired ΔSharpe vs B1 (Aave hold)

| Policy | Mean ΔSharpe | 95% CI | p | Directional consistency |
|---|---:|---:|---:|---:|
{% for r in delta_results -%}
| {{ r.policy }} | {{ "%.2f"|format(r.delta_mean) }} | [{{ "%.2f"|format(r.ci_low_95) }}, {{ "%.2f"|format(r.ci_high_95) }}] | {{ "%.3f"|format(r.nominal_p) }} | {{ r.directional_consistency }} / {{ n_windows }} |
{% endfor %}

![Walk-forward heatmap](../../results/institutional/figures/walk_forward_heatmap.png)
{%- endif %}
