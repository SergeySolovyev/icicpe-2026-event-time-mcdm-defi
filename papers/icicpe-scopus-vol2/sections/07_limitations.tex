% V2 Limitations: event-time T1/T2/T3 ladder on 6-way panel.
% Refreshed 2026-05-28: dropped legacy BiGRU-CNN findings (V1 path), kept
% only the live methodology constraints relevant to the event-time stack.

\section{Limitations and Threats to Validity}
\label{sec:limitations}

We disclose every methodological constraint affecting the headline
results so readers can scope conclusions appropriately and so that
reproductions can target the same envelope.

\textbf{(i) Walk-forward $N = 6$ windows.}  Per-window
$\Delta$APY standard errors are $\sim 5$--$10$\,bp; T3-over-T1 mean
$+7.03$\,bp clears one-sided $p<0.05$ with CI floor $+0.48$\,bp.
Overlapping 12-week windows with overlap-corrected bootstrap would
tighten the CI; deferred to the journal extension.

\textbf{(ii) F4 gas placeholder.}  The F4 \texttt{gas\_log10}
sub-signal uses a constant $25$\,gwei pending an Etherscan key
with sufficient quota; the zero-variance filter drops the column,
leaving F4 with only ETH/USD and USDC peg. F4 thus contributes
\emph{zero} OOF C-index above F3-only --- a methodological, not
empirical, finding. Real per-block gas re-run is queued.

\textbf{(iii) F2 mempool deferred.}  The MacKenzie F2
``order-book dynamics'' class requires paid Blocknative or
archived mempool ($\sim 30$~TB); not available in this study.

\textbf{(iv) Active panel is 3-of-6.}  T1/T2/T3 actively switch
among Aave V3, Morpho Blue, Euler V2 (per-block grid); the other
three protocols enter the matrix only as passive holds.

\textbf{(v) Capacity is analytic.}  The capacity sweep applies the
linearized Krause-2005 yield-impact rather than re-running the
replay engine at each position size; the time-share-weighted
average smooths the small-pool tail.

\textbf{(vi) Pre-mainnet live track record.}  The agent is
verified at Sepolia paper-trade scale; the Institutional Dossier
specifies a five-phase ramp ($\$10$K~$\to$~LP allocation) before
mainnet attestation extends this paper.

% --------------------------------------------------------------
% Code and data availability section (modeled on the user's
% companion ICICPE paper main_v11.pdf, p.6) — explicit reproducibility
% artifact pointers. All URLs are placeholders to be replaced with
% concrete deposits at acceptance / camera-ready.
% --------------------------------------------------------------

\paragraph{Code and data availability.}  All artefacts are public.
End-to-end reproducible pipeline (data extension, T3 training, walk-
forward, capacity, paper rebuild) including the per-block panel and
walk-forward equity parquets:
\url{https://github.com/SergeySolovyev/icicpe-2026-event-time-mcdm-defi}
(\texttt{python -m scripts.finalize\_6way\_pipeline}). T3 Cox-hazard
training artefacts (per-ablation sidecars,
\texttt{results/models/t3\_cox\_*.json}) and Institutional Dossier
(8 chapters + LP slide deck, \texttt{docs/institutional/}) live in
the same repository. Production live agent (per-block loop,
Flashbots private mempool, Tier 5 observability with Prometheus
\texttt{/metrics} + append-only audit trail; $128/128$ tests pass):
\url{https://github.com/SergeySolovyev/event-time-mcdm-agent}.
